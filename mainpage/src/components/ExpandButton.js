import React from "react"

export function  ExpandButton({onClick}){
    return (
        <button
            className="details-button"
            onClick={onClick}
            >details
        </button>
    )
} 
