import React from "react"

export function  DetailsButton({onClick}){
    return (
        <button
            className="details-button"
            onClick={onClick}
            >details
        </button>
    )
} 
